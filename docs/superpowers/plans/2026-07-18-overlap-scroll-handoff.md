# 重叠衔接实施计划

1. 为正式页面首屏增加 sticky 舞台包装层，并为测试加入结构契约。
2. 将滚动行程改为“外层高度减去视口高度”，计算 `smoothstep` 视觉进度与 handoff 进度。
3. 让 Canvas 粒子以 sticky 舞台尺寸采样，避免外层增高后画布被拉长。
4. 用负外边距、顶部羽化层和 GPU transform 实现项目页提前进入。
5. 同步更新原型页的 easing 与羽化遮罩。
6. 执行 Node 语法检查、契约测试、浏览器上下滚动与移动端检查。
7. 提交并推送至 GitHub `main`。
